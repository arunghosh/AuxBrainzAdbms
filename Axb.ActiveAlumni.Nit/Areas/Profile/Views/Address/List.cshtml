@model int
<h4><i class="icon-map-marker"></i> Location</h4>
<ul class="ltype-none ml-prof">
    <li class="span clearb ctnt-bhr">
        <div class="bld wd-lt des-head fs-l span">Current Location</div>
        <div id='currentCtnr'>
            @{Html.RenderAction("Current", Model);}
        </div>
    </li>
    <li class="span clearb ctnt-bhr">
        <div class="bld wd-lt des-head fs-l span">Home Town</div>
        <div id='permanentCtnr'>
            @{Html.RenderAction("Permanent", Model);}
        </div>
    </li>
</ul>
